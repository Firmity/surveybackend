# Core backend — versions verified to install + import together.
fastapi==0.138.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
supabase==2.31.0
google-genai==2.10.0

# Report rendering (Phase 5b) lives in requirements-report.txt — install later.
# WeasyPrint needs extra system libraries (GTK) on Windows, so it's kept
# separate to avoid blocking the core setup.
